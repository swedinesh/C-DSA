#include<stdio.h>

int main() {
    int age;
    printf("enter your age :");
    scanf("%d",&age);
    if(age>=18) {
        printf("You are Eligible for votting...!");
    }
    else{
    ("You are not Eligible for votting...!");
    }

    return 0;
}